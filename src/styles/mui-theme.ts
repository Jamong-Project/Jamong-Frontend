import { createTheme } from "@mui/material";

const muiTheme = createTheme({
  palette: {
    primary: {
      main: "#e76f51",
    },
    secondary: {
      main: "#f4a261",
    },
  },
});

export default muiTheme;
